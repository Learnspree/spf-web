import { MeanResponseModel } from './mean-response-model';

describe('MeanResponseModel', () => {
  it('should create an instance', () => {
    expect(new MeanResponseModel()).toBeTruthy();
  });
});
